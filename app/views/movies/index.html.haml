
-# app/views/movies/index.html.haml

%h2 All Movies

%table#movies
  %thead
    %tr
      %th
      %th Movie Title
      %th Rating
      %th Release Date
      %th More Info
  %tbody 
    -#Ejercicio 7: ordenadas alfabeticamente
    - @movies = @movies.sort_by {|movie| movie.title.downcase}

    - @movies.each_with_index do |movie, index|
      %tr
        %td= index + 1
        %td= movie.title
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path



